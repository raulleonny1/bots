<nav class="navbar navbar-expand-lg navbar-dark site-nav">
  <div class="container">
    <a class="site-nav__brand navbar-brand" href="/">
      <img src="/assets/logo.png" alt="Logo" class="site-nav__logo" width="44" height="44">
      <span>
        <span class="site-nav__title d-block"><%= botName %></span>
        <span class="site-nav__subtitle">Panel parroquial</span>
      </span>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMain" aria-label="Menú">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navMain">
      <ul class="navbar-nav me-auto ms-lg-3">
        <li class="nav-item">
          <a class="nav-link<%= typeof activeNav !== 'undefined' && activeNav === 'dashboard' ? ' active' : '' %>" href="/">
            <i class="bi bi-speedometer2 me-1"></i>Estado
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link<%= typeof activeNav !== 'undefined' && activeNav === 'messages' ? ' active' : '' %>" href="/messages">
            <i class="bi bi-chat-dots me-1"></i>Mensajes
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link<%= typeof activeNav !== 'undefined' && activeNav === 'menu' ? ' active' : '' %>" href="/menu">
            <i class="bi bi-list-ul me-1"></i>Menú
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link<%= typeof activeNav !== 'undefined' && activeNav === 'automations' ? ' active' : '' %>" href="/automations">
            <i class="bi bi-sliders me-1"></i>Automatizaciones
          </a>
        </li>
      </ul>
      <form action="/logout" method="POST" class="d-flex">
        <button class="btn btn-outline-light btn-sm" type="submit">
          <i class="bi bi-box-arrow-right me-1"></i>Salir
        </button>
      </form>
    </div>
  </div>
</nav>
<main class="container">
